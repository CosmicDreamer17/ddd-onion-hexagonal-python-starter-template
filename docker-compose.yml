services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - app-data:/app/data
    env_file:
      - .env
    environment:
      - DATABASE_URL=sqlite:///data/app.db

volumes:
  app-data:
